# Line Follower Parameters for Gazebo Simulation
# Based on Lesson 4 Tutorial defaults (walking_param_sim.yaml)

# Image processing parameters
image_process_size: [160, 120]  # Width x Height for processing

# ROI (Region of Interest) for line detection
# Single ROI in lower portion of image: [y_min, y_max, x_min, x_max] in normalized coordinates (0.0 to 1.0)
line_roi:
  - [0.6, 0.8, 0.2, 0.8]  # Lower ROI (60%-80% vertical, 20%-80% horizontal) - closer to robot

# Color detection thresholds (HSV color space)
# For black line detection
lower_black: [0, 0, 0]      # H, S, V lower bounds
upper_black: [180, 255, 50]  # H, S, V upper bounds
min_contour_area: 80        # Minimum area to consider - reduced to detect line easier

# Walking control parameters - Lesson 4 Tutorial defaults
# Source: Reference/ainex_driver/ainex_kinematics/config/walking_param_sim.yaml
forward_speed: 0.05         # Forward step (meters) - Tutorial example: 0.05m (x_move_amplitude)
min_forward_speed: 0.02     # Reduced speed when sharp turn
turn_gain: 1.2              # Turning sensitivity (not used in Reference implementation)
max_turn_angle: 10.0        # Maximum turning angle (degrees) - Tutorial default
min_turn_angle: 5.0         # DSP switch threshold
center_tolerance: 10        # Dead zone in pixels
center_x_offset: 0          # Camera calibration offset in pixels
walking_speed: 4            # Walking speed (1-4, ignored - we use DSP periods directly)
body_height: 0.025          # Body height offset (meters) - init_z_offset from tutorial
