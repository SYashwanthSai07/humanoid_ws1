<launch>
  <!-- Spawn red ball model at (0.5, 0.5, 0) -->
  <node
    name="spawn_red_ball"
    pkg="gazebo_ros"
    type="spawn_model"
    args="-sdf -model red_ball -x 0.25 -y 0.25 -z 0.0 -file $(env HOME)/model_editor_models/red_ball/model.sdf"
    output="screen" />
</launch>
